# shiny app for Coursera Developing knowledge Product course project # Jun Wang # 6/20/2017 library(shiny) library(plotly) # outline UI for application that attracts a bar graph shinyUI(fluidPage( # Application title titlePanel("Daily air quality measurements in the big apple, might to Gregorian calendar month 1973"), # Sidebar with a slider input for variety of bins sidebarLayout( sidebarPanel( #h2 <- "Choose that variable to explore", radioButtons("typeInput", "Influencing Factor", selections = c("Solar.R", "Wind", "Temp"), selected = "Temp"), #h3 <- "Select month of data", sliderInput("month", "Select Month:", min = 5, max = 9, step = 1, worth = c(5,9)), #submitButton("Go") h2 <- "Instruction: This Shiny app permits you to envision the connection between gas content and influencing factors together with radiation, Wind speed, and temeprature. The app additionally permits you to pick out specific month or many months along. The chosen knowledge utilized in plotting is provided as a table. The visual image is provided as interactive plotly scatterplot. Linear model is additionally displayed. Modelling outline is written further." ), # Show a plot of the generated distribution mainPanel( plotlyOutput("Plot1"), verbatimTextOutput("type"), tableOutput("table") ) ) ))
  
